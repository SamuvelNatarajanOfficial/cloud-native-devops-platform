apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: taskflow
  labels:
    app.kubernetes.io/name: task-service
    app.kubernetes.io/part-of: taskflow
spec:
  selector:
    app.kubernetes.io/name: task-service
  ports:
    - port: 8000
      targetPort: 8000
      name: http
