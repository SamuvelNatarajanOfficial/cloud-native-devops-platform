# EXAMPLE ONLY - placeholder values, safe to commit.
# Do not use these values in any real environment.
# In a later phase this is replaced by a GitOps-managed secret
# (e.g. sealed-secrets or External Secrets Operator) instead of
# a plain Secret checked into version control.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: taskflow
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: taskflow
type: Opaque
stringData:
  POSTGRES_USER: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
