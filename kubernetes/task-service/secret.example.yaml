# EXAMPLE ONLY - placeholder values, safe to commit.
# Do not use these values in any real environment.
apiVersion: v1
kind: Secret
metadata:
  name: task-service-secret
  namespace: taskflow
  labels:
    app.kubernetes.io/name: task-service
    app.kubernetes.io/part-of: taskflow
type: Opaque
stringData:
  DATABASE_URL: "postgresql+psycopg2://REPLACE_ME:REPLACE_ME@postgres:5432/taskflow"
